#include <stdio.h>

int main() {
    int: num, sum = 0;
    
    for(int=i, i=0, i++){
    printf("nh?p 1 so nguyen: %d",i);
    scanf("%d",&num);
    
    if (num % 2 != 0);{
}
    return 0;
}
